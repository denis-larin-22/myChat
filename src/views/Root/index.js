import { Container } from "@mui/material";

import { Header } from "./components/Header";

export const Root = () => {
    return (
        <Container>
            <Header/>
        </Container>
    )
}